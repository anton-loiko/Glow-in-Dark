# Журнал продуктовых решений

Порядок приоритета источников: **этот журнал > дизайн-системы (`docs/design/*.pdf`) > `docs/*.md` (GDD и дополнения)**.
Сюда записывается каждое решение, которое уточняет или отменяет что-то в дизайн-системах или GDD. Формат: дата · решение · что меняется.

## 2026-09-24

| # | Решение | Что меняется |
|---|---------|--------------|
| D1 | Дизайн-системы — источник правды, GDD им подчиняется | GDD обновлён до v1.1 |
| D2 | Стиль арта — **Hi-Res 2D + normal maps** («Биолюминесцентный нуар») | Пиксель-арт 16×16 из GDD 7.1 отменён; `docs/GRAPHICS.md` переписан |
| D3 | Базовый вьюпорт **390×844** | Было 270×480 |
| D4 | Забег — **выживание 10:00** в бесконечном мире из чанков | Спуск вниз до `Exit` отменён |
| D5 | **Баффы Маяков суммируются** между главами и действуют везде (буквально по Meta DS). Скины-награды выдаёт только Маяк главы 1; на тех же тирах в главах 2–3 награды — Кристаллы, сундуки или новые скины | Нужна таблица наград тиров для глав 2–3 (задача баланса); сложность глав 2–3 растёт, чтобы компенсировать стак |
| D6 | **«Сила Огонька» не делаем.** Вкладка «Навыки» (S11) = только «Архив» 12 навыков (описания уровней, рецепты эволюций) | Отменяет первую вкладку S11 из Design System; стоки Искр — Маяк и экипировка |
| D7 | Статы Призрачного Огонька — по Meta DS: **враги наносят −30% урона, свет тухнет на 25% быстрее** | Цифры из S12 Design System (−25% / +15%) не используются |
| D8 | Принимаются предложения дизайн-систем «вне GDD»: шансы Премиум-сундука `[45,32,17,5,1]`, гарант легендарного на 60-м открытии, 5-й слот «Второй амулет» на тире 5 Маяка, `base_cost` Эпика/Легендарки 400/800, лимиты уровней 10/15/20/25/30. **Плакальщик** — только в данных, за выключенным флагом `enemies.mourner_enabled`, арт не делаем до плейтестов | Gear DS и Enemy DS в этих пунктах становятся нормой |
| D9 | **Обменник** (Искры ↔ Кристаллы) убран; его место занимает Премиум-сундук | Блок «Обменник» из S10 не реализуется |
| D10 | **Лидерборды переносятся в V1.1** | Из V1 scope убраны; `LeaderboardManager` и Base-экран удаляются вместе с legacy |
| D11 | Тестовый фреймворк — **gdUnit4** | Юнит-тесты и Монте-Карло в CI |
| D12 | Название проекта во всех документах — **Glow in the Dark** | «Cascade» убран из AGENTS.md, GDSCRIPT.md, TESTING.md |
| D13 | Авторизация: Firebase Anonymous как базовый аккаунт + **нативные гейм-центры**: Game Center (iOS) и Google Play Games Services (Android). Вход в гейм-центр — автоматически при старте, аккаунт гейм-центра привязывается к Firebase-аккаунту | Строка «Аккаунт» в S13 показывает Game Center / Play Games вместо Google/Apple; нужны нативные плагины (см. `tasks/README.md`) |
| D14 | iOS-плагин `ios_plugins/godot_svc` (SFSafariViewController для OAuth из godot-firebase, собран под Godot 3.x) удаляется | Вход через браузер не используется |
| D15 | **No Ads убран как товар.** Принудительной рекламы (интерстишалов) в игре нет, отключать нечего; вся реклама — только Rewarded Video по правилу ▶. Стартер-пак содержит скин и Кристаллы, без «отключения рекламы» | Строка «Без рекламы» в S10 и `ads.interstitial_enabled` не реализуются |
| D16 | **Стек нативных плагинов** (детали — `docs/research/native_plugins.md`): Android IAP — godot-google-play-billing 3.3; Android гейм-центр — godot-play-game-services 3.4; iOS Game Center + StoreKit 2 — GodotApplePlugins; Analytics + Crashlytics — godot-x/firebase 3.1; Firebase Auth и Firestore — REST-аддон `godot-firebase` (Anonymous + `signInWithGameCenter`; Play Games — через прототип, запасной путь Cloud Function). **Минимальная iOS — 17**, эталон производительности iOS — **iPhone XR** (вместо iPhone 8 из Design System §08). Рекламный SDK — см. D17 | Эталонные устройства в GDD и задачах обновлены; первым шагом разработки — прототип интеграций (5 шагов из отчёта) |
| D17 | **Реклама — Appodeal** (медиация с автоматическим выбором самой доходной сети). Официального плагина под Godot 4 нет → пишем **свою тонкую обёртку на обе платформы**: iOS — Swift/SwiftGodot (по образцу GodotApplePlugins), Android — Kotlin (по образцу damnedpie/godot-appodeal). Наружу только нужное V1: инициализация, Rewarded (загрузка, показ, колбэки), форма согласий Appodeal ConsentManager (GDPR/UMP) и ATT на iOS. Интерстишалов и баннеров нет (D15). Обновление SDK и адаптеров — плановое, 1–2 раза в год под требования сторов | AdMob-плагин poing (`addons/admob`, `ios/plugins/poing-godot-admob*`) удаляется; `AdManager` работает через бэкенд-адаптер и не знает о SDK; в прототип добавлена проверка конфликта Firebase-зависимостей Appodeal с godot-x/firebase |
| D18 | **Лунный Огонёк** — эксклюзив стартер-пака, класс **РИТМ** «Фазы Луны». Цвет света `#D8D0FF` (перламутрово-лавандовый; `#BFD4FF` остаётся за Призрачным). Цикл 20 с: телеграф 1 с (свет сгущается, звук) → **Полнолуние** 6 с: радиус света +30%, урон ауры +40% → **Новолуние** 6 с: радиус −20% → обычный свет. Модификаторы радиуса перемножаются с Фокусной линзой и Энергоёмкостью, но итоговый множитель радиуса ограничен **×2.0 от базового** (`skins.json.moon.radius_cap`). Стартер-пак: Лунный + 500 ◆, однократно, FOMO-таймер 24 ч | Закрыт вопрос о скине стартер-пака; Лунный добавлен в S12/S14, `skins.json`, `shop.json`; rim врагов = `#D8D0FF` |
